#include "headerf.h"
/**
  * main - entry point
  * Return: return 0
  */
int main(void)
{
char in_put[MAX_INPUT];
while (1)
{
mydispfunx_edna();
myreadfunx_edna(whattoread, sizeof(whattoread));
myexecfunx_edna(whattoexe);
}
return (0);
}
